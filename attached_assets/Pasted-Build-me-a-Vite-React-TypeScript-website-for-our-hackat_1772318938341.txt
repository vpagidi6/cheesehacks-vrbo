Build me a Vite + React + TypeScript website for our hackathon project called “WaterWise AI”. This site shows a dashboard for estimated water usage from AI prompts. We have a FastAPI backend already working.

STACK / REQUIREMENTS
- Vite + React + TypeScript (react-ts template)
- Use react-router-dom for routing
- Email-only hackathon login using localStorage key "water_user" (no auth providers)
- All data comes from our FastAPI backend (NO dummy JSON)
- Clean, demo-ready UI with simple card layout, good spacing, readable typography
- Use basic CSS (either a global CSS file or inline styles). Avoid heavy UI libraries unless trivial.

BACKEND API (must match exactly)
Base URL should be configurable via environment variable:
- Use `VITE_API_BASE` (default to "http://localhost:8000" if not set)

Endpoints:
1) GET `${API_BASE}/api/summary?user=email&month=YYYY-MM`
Returns type:
type SummaryResponse = {
  today: { ml: number; tokens: number; date: string };
  dailyLimitMl: number;
  estimationMode: "low" | "conservative" | "range";
  monthDays: Array<{ date: string; ml: number; tokens: number }>;
  byProvider: { chatgpt: number; claude: number; gemini: number };
};

2) POST `${API_BASE}/api/settings`
Body:
{ user: string; dailyLimitMl: number; estimationMode: "low"|"conservative"|"range" }
Returns:
{ ok: true }

PAGES
1) /login
   - input email + Continue button
   - store email in localStorage "water_user"
   - redirect to /dashboard

2) /dashboard
   - Determine current month string "YYYY-MM" from the user’s local time
   - Call fetchSummary(month) on load
   - Show cards:
     A) Today card:
        - show today ml and tokens
        - progress bar vs dailyLimitMl
        - red warning banner if today.ml >= dailyLimitMl
        - show estimationMode
     B) By Provider card:
        - list ChatGPT / Claude / Gemini with monthly mL totals
     C) Monthly Calendar card:
        - calendar heatmap grid for the month based on monthDays
        - greener = higher ml
        - hover tooltip with date + ml + tokens
   - Include loading + error states (retry button if error)

3) /settings
   - On page load, also call fetchSummary(currentMonth) to prefill dailyLimitMl + estimationMode
   - Allow editing dailyLimitMl and estimationMode
   - Save button calls POST /api/settings
   - Show “Saved!” status or error message

NAVIGATION
- top nav bar with links: Dashboard, Settings, Logout
- logout clears localStorage and routes to /login
- route guard: if not logged in, redirect to /login

API LAYER
Create:
- src/api/types.ts (SummaryResponse + EstimationMode types)
- src/api/client.ts
  - getUser(), setUser(), clearUser()
  - fetchSummary(month: string): Promise<SummaryResponse>
  - saveSettings(dailyLimitMl: number, estimationMode: EstimationMode): Promise<void>
All fetch calls should:
- throw readable errors on non-200
- use encodeURIComponent for user
- include Content-Type: application/json for POST

COMPONENTS
Create reusable components:
- ProgressBar(value, max)
- ProviderBreakdown(byProvider)
- CalendarHeatmap(month, days)
CalendarHeatmap requirements:
- Render a 7-column grid
- Fill empty leading cells so day 1 starts on correct weekday
- Color scale based on max ml for that month
- Each cell shows day number
- title tooltip shows "YYYY-MM-DD: X mL, Y tokens"

DELIVERABLE
- Generate all files and ensure `npm install` and `npm run dev` works.
- Add a short README.md:
  - set VITE_API_BASE
  - run dev server
  - describe the 3 pages

Implement now.